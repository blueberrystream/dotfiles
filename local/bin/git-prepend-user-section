#!/bin/bash

replacement="__alias__"
email="kid0725${replacement}@gmail.com"
if [ -n "$1" ]; then
  email=`echo ${email/${replacement}/+$1}`
else
  email=`echo ${email/${replacement}/}`
fi

# put user section
git config --local user.name "KID the Euforia"
git config --local user.email $email
